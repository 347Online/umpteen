# Sample Program: fib.um

fnc fib(n: Number) -> Number {
  if n <= 1 {
    return n;
  }

  return fib(n - 1) + fib(n - 2);
}

var i = 0;
let iterations = 20;

loop {
  print(fib(i));
  i += 1;

  if i >= iterations {
    break;
  }
}